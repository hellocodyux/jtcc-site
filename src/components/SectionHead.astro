---
interface Props { heading: string; intro?: string; }
const { heading, intro } = Astro.props;
---
<div class="reveal mb-16 max-w-2xl">
  <h2 class="text-4xl md:text-5xl">{heading}</h2>
  {intro && <p class="mt-5 text-lg leading-relaxed text-choc/75">{intro}</p>}
</div>
